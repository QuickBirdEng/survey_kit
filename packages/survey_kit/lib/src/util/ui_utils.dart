import 'package:flutter/material.dart';

/// A vertical [SizedBox] with 12 px height, used for standard spacing between
/// elements.
const smallVerticalSpacer = SizedBox(
  height: 12,
);

/// A vertical [SizedBox] with 8 px height, used for compact spacing between
/// elements.
const verySmallVerticalSpacer = SizedBox(
  height: 8,
);
